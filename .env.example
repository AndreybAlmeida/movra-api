# ── Banco de dados ─────────────────────────────────────────────
DATABASE_URL=postgres://user:password@host:5432/movra

# ── Auth ───────────────────────────────────────────────────────
JWT_SECRET=troque_por_uma_chave_longa_e_aleatoria_aqui

# ── Ambiente ───────────────────────────────────────────────────
NODE_ENV=development
PORT=3000

# ── Pagamentos (opcional — mock se não definido) ────────────────
# PAYMENT_PROVIDER=bancard
# BANCARD_TOKEN=seu_token_aqui

# ── WhatsApp (opcional — log se não definido) ───────────────────
# WHATSAPP_PROVIDER=twilio
# TWILIO_SID=seu_sid_aqui
# TWILIO_TOKEN=seu_token_aqui
# TWILIO_WHATSAPP_FROM=+1415...

# ── Câmbio (usa API pública gratuita — sem chave necessária) ────
# EXCHANGE_RATE_API_KEY=opcional_se_quiser_plano_pago
